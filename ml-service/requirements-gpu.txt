# TripoSR GPU dependencies
# Install on top of base requirements: pip install -r requirements-gpu.txt

# Base dependencies
-r requirements.txt

# PyTorch (CUDA 11.8) — adjust for your CUDA version
--extra-index-url https://download.pytorch.org/whl/cu118
torch==2.1.0+cu118

# TripoSR model dependencies
omegaconf==2.3.0
einops==0.7.0
transformers==4.35.0
huggingface-hub
imageio[ffmpeg]

# Background removal
rembg

# Texture baking (optional)
xatlas==0.0.9
moderngl==5.10.0
