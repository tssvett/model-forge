# TripoSR dependencies (TripoSR itself is cloned via git in Dockerfile)
# The repo has no setup.py/pyproject.toml, so it cannot be pip-installed.
omegaconf==2.3.0
einops==0.7.0
transformers==4.36.0
huggingface-hub==0.20.3
imageio[ffmpeg]

# Background removal
rembg==2.0.50

# Texture baking
xatlas==0.0.9
moderngl==5.10.0
