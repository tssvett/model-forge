# TripoSR model and its dependencies
# Note: not pinned to commit because repo lacks setup.py at HEAD;
# pip installs from default branch which has the installable structure
git+https://github.com/VAST-AI-Research/TripoSR.git

# TripoSR model dependencies
omegaconf==2.3.0
einops==0.7.0
transformers==4.36.0
huggingface-hub==0.20.3
imageio[ffmpeg]

# Background removal
rembg==2.0.50

# Texture baking
xatlas==0.0.9
moderngl==5.10.0
